[package]
name = "brickroll"
version = "0.1.0"
edition = "2021"
description = "A Brainfuck to Rickroll compiler"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clap = { version="4.1.6", features=["derive"] }
